import React from 'react';
import ResourceList from '../components/ResourceList';

const Datasets = () => (
  <div>
    <h2>Datasets</h2>
    <ResourceList filterType="dataset" />
  </div>
);

export default Datasets;